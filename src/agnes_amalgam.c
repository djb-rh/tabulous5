/* agnes lives in third_party/ so it stays visibly unmodified — pulling it in
   through one shim keeps PlatformIO's src filter simple and makes it obvious
   that nothing here patches it. */
#include "agnes.c"
