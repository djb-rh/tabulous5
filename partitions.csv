# Tabulous5 — 16 MB ESP32-P4 flash layout.
# No OTA: one 6 MB app slot, 64 KB for a core dump, and the rest to LittleFS
# for word packs, sound effects and built-in ROMs (~10.3 MB). The data
# partition must be named "spiffs" — that's the label Arduino's
# LittleFS.begin() looks for by default.
#
# The core dump sits at the very END of flash so that adding it did not move
# the app or the filesystem: everything before it keeps the offset it had.
# The firmware is built with core dumps to flash already enabled, so until
# this partition existed every crash printed "No core dump partition found!"
# and was lost. See tools/coredump.
# Name,     Type, SubType,  Offset,   Size,     Flags
nvs,        data, nvs,      0x9000,   0x5000,
phy_init,   data, phy,      0xe000,   0x1000,
factory,    app,  factory,  0x10000,  0x600000,
spiffs,     data, spiffs,   0x610000, 0x9E0000,
coredump,   data, coredump, 0xFF0000, 0x10000,
